[pytest]
# Use importlib import mode to avoid conftest.py path collisions
# when running tests across multiple vendor directories.
# pytest 9.x does not honour the ini key reliably, so pass via addopts.
addopts = --import-mode=importlib
